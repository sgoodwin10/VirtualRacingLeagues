<?xml version="1.0"?>
<ruleset name="Laravel Standards">
    <description>Laravel project coding standard</description>

    <!-- Files to check -->
    <file>app</file>
    <file>config</file>
    <file>database</file>
    <file>routes</file>
    <file>tests</file>

    <!-- Exclude patterns -->
    <exclude-pattern>*/cache/*</exclude-pattern>
    <exclude-pattern>*/storage/*</exclude-pattern>
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/bootstrap/*</exclude-pattern>
    <exclude-pattern>*.blade.php</exclude-pattern>
    <!-- Exclude old-style migrations without namespaces (from packages) -->
    <exclude-pattern>database/migrations/2025_10_14_085708_create_activity_log_table.php</exclude-pattern>
    <exclude-pattern>database/migrations/2025_10_14_085709_add_event_column_to_activity_log_table.php</exclude-pattern>
    <exclude-pattern>database/migrations/2025_10_14_085710_add_batch_uuid_column_to_activity_log_table.php</exclude-pattern>
    <exclude-pattern>database/migrations.backup/*</exclude-pattern>

    <!-- Use PSR-12 as base standard -->
    <rule ref="PSR12"/>

    <!-- Allow snake_case test method names (Laravel convention) -->
    <rule ref="PSR1.Methods.CamelCapsMethodName">
        <exclude-pattern>tests/*</exclude-pattern>
    </rule>

    <!-- Show progress and colors -->
    <arg name="colors"/>
    <arg value="sp"/>
</ruleset>
