# Implementation Plan: Add Platform Logo to Division/Standings Tables

**Date**: 2026-02-08
**Component**: `SeasonDetailWhiteLabelView.vue`
**Agent**: `dev-fe-public`

---

## Overview

Add the `gran-turismo-7-logo.svg` platform logo to the left of headings in the three standings table contexts within the whitelabel season detail view. The logo is a static asset at `public/images/platforms/gran-turismo-7-logo.svg`.

---

## Current State

The `SeasonDetailWhiteLabelView.vue` has three standings table sections:

1. **Division Tables** (line 82-180): Each division renders `<h4 class="division-title">{{ division.division_name }}</h4>` above the table
2. **Flat Drivers Table** (line 182-277): No sub-heading — table renders directly under the shared "Championship Standings" section title
3. **Teams Championship Table** (line 376-436): No sub-heading — table renders directly within the "teams" tab

The platform logo file exists at `public/images/platforms/gran-turismo-7-logo.svg`.

---

## Implementation Steps

### Phase 1: Add platform logo to Division Tables heading

**File**: `resources/public/js/views/leagues/SeasonDetailWhiteLabelView.vue`

**Location**: Line 89 — the `<h4 class="division-title">` element

**Change**: Wrap the division title content in a flex container with the logo:

```html
<!-- Before -->
<h4 class="division-title">{{ division.division_name }}</h4>

<!-- After -->
<h4 class="division-title">
  <img src="/images/platforms/gran-turismo-7-logo.svg" alt="Gran Turismo 7" class="platform-logo" />
  {{ division.division_name }}
</h4>
```

### Phase 2: Add platform logo to Flat Drivers Table

**Location**: Line 186 — just before the `<table class="standings-table">` inside the flat drivers table wrapper (line 183-277)

**Change**: Add a heading with the platform logo before the table. Since the flat table has no sub-heading, add a `<div>` with the logo similar to the division title styling:

```html
<!-- Inside the flat table wrapper, before the <table> element -->
<h4 class="division-title">
  <img src="/images/platforms/gran-turismo-7-logo.svg" alt="Gran Turismo 7" class="platform-logo" />
  Championship Standings
</h4>
```

Also add the same for the **Drivers Tab (when teams championship exists)** at line 284 — same approach:

```html
<h4 class="division-title">
  <img src="/images/platforms/gran-turismo-7-logo.svg" alt="Gran Turismo 7" class="platform-logo" />
  Drivers Championship
</h4>
```

### Phase 3: Add platform logo to Teams Championship Table

**Location**: Line 382 — inside the teams table wrapper, before `<table class="standings-table teams-table">`

**Change**: Add a heading with the platform logo:

```html
<h4 class="division-title">
  <img src="/images/platforms/gran-turismo-7-logo.svg" alt="Gran Turismo 7" class="platform-logo" />
  Team Championship
</h4>
```

### Phase 4: Add CSS for platform logo

**Location**: CSS section, near the `.division-title` styles (around line 2780)

**Change**: Update `.division-title` to use flexbox and add `.platform-logo` styles:

```css
.division-title {
  font-size: 1.125rem;
  font-weight: 700;
  color: var(--wl-text-primary);
  padding: 10px 12px;
  display: flex;
  align-items: center;
  gap: 0.5rem;
}

.platform-logo {
  height: 24px;
  width: auto;
  object-fit: contain;
  flex-shrink: 0;
}
```

---

## Files Modified

| File | Change |
|------|--------|
| `resources/public/js/views/leagues/SeasonDetailWhiteLabelView.vue` | Add platform logo img + CSS in 4 template locations + 1 CSS addition |

---

## Out of Scope

- No backend API changes needed (static logo path)
- No changes to `StandingsTable.vue` component (used in non-whitelabel view)
- No changes to `CrossDivisionAllTimesTable.vue`
- No TypeScript type changes
- Not making the logo dynamic based on platform (hardcoded to GT7 for now)

---

## Success Criteria

### Automated Verification:
- [ ] All Vitest tests pass: `.claude/support/scripts/vitests_run_tests.sh`
- [ ] TypeScript type checking passes: `npm run type-check`
- [ ] ESLint passes: `npm run lint`
- [ ] Formatting passes: `npm run format`

### Manual Verification:
- [ ] GT7 logo appears to the left of each division name in division tables
- [ ] GT7 logo appears in the flat table heading
- [ ] GT7 logo appears in the teams championship table heading
- [ ] Logo is properly sized (24px height) and vertically aligned with text
- [ ] Logo does not break responsive layout on mobile
