# Overview
Update the authentication pages (login, register, lost password, reset password) to the new design schema for the Public Dashboard.


# Goal
To update the design/layout/ux/ui to align with the `resources/public/js/views/HomeView.vue` design and colour schema.


# Agent
- Use agent `dev-fe-public` for front end work
- Use agent `dev-be` for backend work if required.


## Important
- Use the existing colour schema
- Use existing components found in `resources/public/js/components/common`
- Kepp the same Backend flow as it works already.
- Do not create custom css styles. Use Tailwind CSS


# Tasks
Include the `Homeview` footer and navigation in these pages.

## 1. Login Page
- existing page: `resources/public/js/views/auth/LoginView.vue`


## 2. Forgot Password Page
- existing page: `resources/public/js/views/auth/ForgotPasswordView.vue`


## 3. Reset Password Page
- existing page: `resources/public/js/views/auth/ResetPasswordView.vue`


## 4. Register Pages
- existing page: `resources/public/js/views/auth/RegisterView.vue`


## 5. Update Email templates
Update the email templates. 
Have a white background, but look to improve if you can. 
This is optional


# Plan
Create a plan in `docs/PublicDashboard/design-2026/design-1-velocity/authenticate`. Create a overview plan, then link any other plans you create to it.