Read $ARGUMENTS. 
Look at all the components in this directory and its subdirectories. if any component uses custom css styles in the component, i want you to convert them to use tailwind css classes.
Some custom css functions are not possible with Tailwind CSS, so custom css styles are allowed in this case.
use which ever of the following agents - @dev-fe-app, @dev-fe-public, @dev-fe-admin - to update.