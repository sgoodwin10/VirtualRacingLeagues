/**
 * Pagination constants for DataTables and lists
 */

/**
 * Default number of rows per page for DataTables
 */
export const DEFAULT_ROWS_PER_PAGE = 10;

/**
 * Available rows per page options for DataTables
 */
export const ROWS_PER_PAGE_OPTIONS = [10, 25, 50];

/**
 * Default color range steps for round coloring
 */
export const DEFAULT_COLOR_RANGE_STEPS = 10;
